import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF225149); // Verde escuro
  static const Color accent = Color(0xFFDBB59E);  // Bege claro
  static const Color background = Color(0xFFF2E6DA); // Fundo claro
  static const Color highlight = Color(0xFF93A491); // Verde claro
  static const Color alert = Color(0xFFCA6A52);     // Vermelho
  static const Color textDark = Color(0xFF464C52);  // Cinza escuro
  static const Color white = Colors.white;          // Branco
  static const Color black = Colors.black;          // Preto
}
